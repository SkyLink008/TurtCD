# Инструкция по запуску проекта

## 1. Установка Python

Для корректной работы проекта требуется установленный **Python версии 3.9 или выше**.

Если Python ещё не установлен, скачайте установщик с официального сайта:

👉 [https://www.python.org/downloads/](https://www.python.org/downloads/)

Во время установки **обязательно включите пункт "Add Python to PATH"**, чтобы можно было запускать Python из командной строки.

Проверьте установку, открыв терминал (или командную строку) и выполнив:

```bash
python --version
```

или, если первая команда не сработала:

```bash
py --version
```

Вы должны увидеть сообщение вида:

```
Python 3.10.11
```

---

## 2. Загрузка или клонирование проекта

### Вариант 1. Скачивание архива

Если вы получили проект в виде ZIP-архива, просто распакуйте его в удобную папку, например `C:\MyProjects\turtcd`.

### Вариант 2. Клонирование из репозитория

Если проект размещён в GitHub или другом репозитории, используйте команду:

```bash
git clone https://github.com/SkyLink008/TurtCD.git
cd TurtCD
```

---

## 3. Создание и активация виртуального окружения

Чтобы изолировать зависимости проекта от системы, создайте виртуальную среду Python.

Перейдите в папку проекта и выполните следующие команды:

### Windows:

```bash
python -m venv venv
```

После этого активируйте виртуальное окружение:

```bash
venv\Scripts\activate
```

Если всё прошло успешно, в начале строки терминала появится надпись `(venv)`.

Для выхода из окружения используйте:

```bash
deactivate
```

---

### Linux / macOS:

Создание виртуального окружения:

```bash
python3 -m venv venv
```

Активация окружения:

```bash
source venv/bin/activate
```

Отключение:

```bash
deactivate
```

---

## 4. Установка зависимостей

После активации виртуальной среды установите все необходимые библиотеки.

Все зависимости проекта перечислены в файле **requirements.txt**.
Для установки выполните команду:

```bash
pip install -r requirements.txt
```

Эта команда автоматически установит:

* Flask (для работы веб-сервера),
* telebot (для Telegram-бота, если он используется),
* pyinstaller (для компиляции в exe),
* а также все другие зависимости, указанные в основном файле `main.py`.

Проверить успешную установку можно командой:

```bash
pip list
```

---

## 5. Запуск проекта

### Вариант 1: Упрощённый запуск через лаунчер (рекомендуется)

Для удобного запуска проекта используйте графический лаунчер:

1. **Запустите файл `launcher.exe`** (двойной клик в проводнике Windows)

2. **Дождитесь загрузки всех компонентов.** Лаунчер автоматически:
   - Проверит наличие Python на вашем компьютере
   - Установит зависимости из `requirements.txt` (если они ещё не установлены)
   - Предоставит простой интерфейс для запуска сервера
   
   > ⚠️ **Важно:** Дождитесь завершения всех проверок и установки зависимостей. Кнопка **"▶ Запустить сервер"** станет активной только после того, как все компоненты будут готовы. Следите за сообщениями в области статуса лаунчера.

3. После того, как кнопка станет активной, нажмите **"▶ Запустить сервер"** в окне лаунчера

4. Браузер откроется автоматически, и вы сможете работать с проектом

5. Для остановки сервера нажмите кнопку **"⏹ Завершить работу"**

> **Примечание:** Если у вас нет файла `launcher.exe`, его можно создать, скомпилировав `source/launcher.py` с помощью PyInstaller.

---

### Вариант 2: Традиционный запуск через командную строку

После установки зависимостей запустите проект:

```bash
python main.py
```

или (если используется `py`):

```bash
py main.py
```

Если всё прошло успешно, вы увидите сообщение примерно такого вида:

```
* Running on http://127.0.0.1:5000 (Press CTRL+C to quit)
```

Теперь можно открыть браузер и перейти по адресу:

```
http://127.0.0.1:5000/
```

Если в конфигурации файла `main.py` указано `"is_host": true`, то сервер будет доступен также в локальной сети по IP-адресу вашего компьютера.

---

## 6. Возможные ошибки

* **Python не найден.**
  Убедитесь, что Python установлен и добавлен в PATH.

* **Ошибка "Flask not found".**
  Проверьте, что зависимости установлены:

  ```bash
  pip install -r requirements.txt
  ```

---

## 7. Остановка сервера

Чтобы остановить сервер, нажмите **Ctrl + C** в консоли, где он запущен.

---

## 8. Дополнительно

Проект поддерживает автоматическую компиляцию Python-кода в исполняемый `.exe` файл с помощью **PyInstaller**.
При первом запуске соответствующего API модуль PyInstaller устанавливается автоматически, если он отсутствует.

---

Готово! После выполнения этих шагов среда будет полностью настроена, и проект можно запускать, разрабатывать и компилировать.
