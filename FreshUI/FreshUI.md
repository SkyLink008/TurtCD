# FreshUI - Модификация для создания графических интерфейсов

## Описание
Модификация FreshUI предоставляет блоки для создания современных графических интерфейсов с использованием библиотеки PySide6 (Qt для Python). С помощью этой модификации вы можете создавать окна, кнопки, поля ввода, списки и другие элементы интерфейса.

## Необходимые библиотеки

Для работы модификации необходимо установить следующие библиотеки в файле `requirements.txt`:

```
PySide6
```

## Как добавить библиотеки в Setup

1. Откройте редактор TurtCD
2. Нажмите на кнопку **Настройки** (иконка шестеренки) в правом верхнем углу
3. В открывшемся окне найдите раздел **Setup**
4. В текстовом поле вы увидите содержимое файла `requirements.txt`
5. Добавьте строку `PySide6` в файл (если её там нет)
6. Нажмите кнопку **💾 Сохранить requirements.txt**
7. Перезапустите лаунчер для применения изменений

## Основные возможности

- Создание окон приложений
- Работа с кнопками, метками, полями ввода
- Создание макетов (вертикальных и горизонтальных)
- Работа с диалоговыми окнами
- Работа с файлами и изображениями
- Создание графических сцен

## Пример использования

1. Используйте блок **"Инициализировать UI"** для импорта необходимых модулей
2. Создайте жизненный цикл приложения с помощью блока **"Создать жизненный цикл"**
3. Создайте окно с помощью блока **"Создать окно"**
4. Добавляйте элементы интерфейса и настраивайте их
5. Запустите приложение с помощью блока **"Запустить жизненный цикл"**

